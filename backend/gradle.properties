# Gradle/Kotlin: use JDK 17–24 to run Gradle (Kotlin compiler does not support JDK 25 as the daemon JVM yet).
# macOS example:
#   export JAVA_HOME=$(/usr/libexec/java_home -v 17)
#   ./gradlew bootRun

org.gradle.jvmargs=-Xmx512m -Dfile.encoding=UTF-8
kotlin.code.style=official
